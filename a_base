#include <iostream>
#include <string>
#define random(min,max) (min + rand() % (max - min + 1))
using namespace std;

template <typename T>
class MyBinaryTree {
private:

public:

};

int main()
{
    setlocale(LC_ALL, "rus");
    srand(time(NULL));
}
